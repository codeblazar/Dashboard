name: Fetch RSS Feeds

on:
  schedule:
    - cron: '0 6,12,18 * * *'   # 6am, 12pm, 6pm UTC
  push:
    branches: [main]
    paths-ignore:
      - 'public/data/feeds.json'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: fetch-feeds
  cancel-in-progress: false

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Fetch RSS feeds
        run: node scripts/fetch-feeds.mjs

      - name: Fetch stock data
        run: node scripts/fetch-stocks.mjs

      - name: Summarise feeds with AI
        run: node scripts/summarize.mjs
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}

      - name: Commit updated data files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add public/data/feeds.json public/data/summaries.json public/data/stocks.json
          git diff --staged --quiet || git commit -m "chore: update feeds.json [skip ci]"
          git push
